<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Whenever we need to display the variable -->
    <!-- Alligator mouth -->
    <title><%=title%></title>
    <script src="https://kit.fontawesome.com/7b6fdf30a8.js" crossorigin="anonymous"></script>
    <!-- our css should be below other css file so that we can override it -->
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <h1 id="page-heading">
        <i class="fa-solid fa-address-book"></i>
        My Contacts List</h1>
    <!-- This html page is shown only when the user want to see -->
    <!-- So, it does not get pre-compiled -->
    <div>
        <ul>
            <%for(let i of contact_list){%>

                <li id="contact-details-container">
                    <div id="contact-details">
                        <p><%=i.name%></p>
                        <p><%=i.phone%></p>
                    </div>
                    <div id="delete-button">
                        <!-- this delete contact can have either string param or simple param or query param. -->
                        <!-- <a href="/delete-contact/<%=i._id%>" > -->
                            <!--  Pass it as query -->
                        <a href="/delete-contact/?id=<%=i._id%>">
                            <i class="fa-solid fa-circle-minus"></i>
                        </a>
                    </div>
                </li>

            <%}%>
        </ul>
    </div>
    <!-- action is the url where you want to send the data -->
    <form action="/create-contact" method="POST">
        <input type="text" name="name" placeholder="Enter Name" required>
        <input type="number" name="phone" placeholder="Enter Phone" required>
        <button type="submit">Add Contact</button>
    </form>
    <script type="text/javascript" src="/js/home.js"></script>
</body>
</html>